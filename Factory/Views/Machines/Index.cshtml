@{
  Layout = "_Layout";
}
@using SillyStingz.Models;

<div id=Header>
  <h1 id=HeaderText>Machines we have:</h1>
</div>

<div id=MainBody>
  @if (@Model.Count == 0)
  {
    <h3>No machines have been added yet!</h3>
  }
<ul>
  @foreach(Machine machine in Model)
  {
    <li>@Html.ActionLink($"{machine.Name}", "Details", new { id = machine.MachineId })</li>
  }
</ul>
<p>@Html.ActionLink("Add new machine", "Create")</p>
<p>@Html.ActionLink("Home", "Index", "Home")</p>

<section>
      <span></span>
      <span></span>
      <span></span>
      <span></span>
      <span></span>
      <span></span>
      <span></span>
      <span></span>
      <span></span>
      <span></span>
    </section>
</div>


